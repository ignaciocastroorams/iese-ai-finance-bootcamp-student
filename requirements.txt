# AI-Augmented Productivity for Finance — IESE MiF
# REQUIRES PYTHON 3.10 OR NEWER. On macOS the built-in `python3` is 3.9 and
# will fail on pydantic-ai-slim; check with `python --version` first.
# Install:  pip install -r requirements.txt
pandas>=2.0
requests>=2.31
matplotlib>=3.8
seaborn>=0.13
plotly>=5.24
anthropic>=1.0
pydantic>=2.7
pydantic-ai-slim[anthropic]>=2.33
langgraph>=1.2
grandalf>=0.8            # lets LangGraph draw its graph as text in the notebook
# NOTE: langchain-anthropic is deliberately NOT installed: it pins anthropic<1.0
# while pydantic-ai requires anthropic>=1.0. Session 5 explains the conflict.
python-dotenv>=1.0
pytest>=8.0
tabulate>=0.9
openpyxl>=3.1
ipykernel>=6.29
nbformat>=5.9

